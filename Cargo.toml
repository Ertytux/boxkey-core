[package]
name = "boxkey-core"
version = "1.0.0"
edition = "2021"
rust-version = "1.70"
description = "Biblioteca criptográfica central del protocolo BoxKey (BZ). DKG, FROST (RFC 9591), Schnorr BIP340, resharing."
license = "MIT"
repository = "https://github.com/Ertytux/boxkey-core"

[lib]
name = "boxkey_core"
path = "src/lib.rs"

[dependencies]
k256 = { version = "0.13", features = ["schnorr"] }
secp256k1 = { version = "0.31", features = ["global-context", "serde"] }
signature = "2"
rand = "0.8"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
zeroize = { version = "1.6", features = ["zeroize_derive"] }
hex = "0.4"
chacha20poly1305 = "0.10"
sha2 = "0.10"
hkdf = "0.12"

[dev-dependencies]
criterion = "0.5"

[[bench]]
name = "signing_bench"
harness = false